#!/bin/bash
# conditionals-script-pract.sh -- looking into conditional syntax

#	 awk '
#	 if () {
#	 
#	 } else if (){
#	 
#	 } else {
#	 
#	 }
#	 '

#	 awk '
#	 if (x) print x
#	 if ( y == x ) print x
#	 if ( x ~ /[yY](es)?/ ) print x
#	 if ( expression ) {
#		multiple actions
#		multiple actions
#		multiple actions
#		}
#	 if ( expression ) action1; [else action2]
#	 # cannot avoid the braces here, if the semicolon is used 	     the 
#	 # braces must follow. 
#	 
#	 if ( avg >= 65 )
#		grade = "Pass"
#	 else
#		grade = "Fail"
#	 # conditional operator ? from C lang
#	 # expr ? action1 : action2
#	 grade = (avg >= 65) ? "Pass" : "Fail"



#'

